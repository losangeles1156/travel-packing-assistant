## Frontend (Vercel)
# Public backend API base URL.
# Example: https://your-backend.zeabur.app
VITE_API_BASE_URL=

# Optional. Enable PWA generation only when explicitly needed.
# Default (unset): disabled to avoid unstable Workbox build failures.
# Set to 1 to enable.
VITE_ENABLE_PWA=

## Backend (Zeabur)
# Comma-separated allow list for browser origins.
# Example: https://travel-packing-assistant.vercel.app,https://travel-packing-assistant-git-main-yourteam.vercel.app
ALLOWED_ORIGIN=http://localhost:3000

# Set in production to protect admin API.
ADMIN_TOKEN=

# Trust proxy setting for IP extraction behind reverse proxy.
# For Zeabur, keep default 1 unless your network topology differs.
TRUST_PROXY=1

# Shared list validity period in days.
SHARE_TTL_DAYS=30

# Auto cleanup policy for analytics data.
EVENT_RETENTION_DAYS=180
AUTO_CLEANUP_INTERVAL_HOURS=24

# Disable by default in production unless explicitly needed.
ENABLE_ADMIN_BACKUP=0

# Zeabur usually injects PORT automatically.
PORT=4000
